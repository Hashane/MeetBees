import 'package:flutter/material.dart';

///For the purpose of Gender selecting custom radio buttons
class Gender {
  String name;
  IconData icon;
  bool isSelected;

  Gender(this.name, this.icon, this.isSelected);
}